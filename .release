Touch this file and push to trigger a signed TestFlight build.
See the `push` trigger in .github/workflows/release.yml.

build 24 — Settings explains the seven fast-capture routes that already
           existed; a second control and Siri phrase for typing
